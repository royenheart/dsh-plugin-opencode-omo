# End-to-end coverage ledger

Generated from `index.json` by dsh-migrate. Rows are written by the agent from the plugin's
documented features; `lastPassedFor` records the newest harness tag this feature was verified against.

Framework: playwright
Generated for: dsh dsh-v0.1.6-alpha.1, plugin @royenheart/dsh-plugin-opencode-omo

| Feature | Surface | Entry | Expected | Checks | State | Last passed |
|---|---|---|---|---|---|---|
| `preset-mode-entry` | web-client | New-session hero: click the agent-mode chip ("Standard mode" by default) | The menu lists opencode-omo with its shipped Chinese description and the chip stages it on the blank session | aria, console, visual | passing | dsh-v0.1.6-alpha.1 |
| `preset-load-health` | web-client | Settings -> Agent presets | The card renders the preset description and the page never reports "Failed to load" (every preset row resolves, including the LSP rows) | aria, console | passing | dsh-v0.1.6-alpha.1 |
| `preset-isolation` | web-client | Blank-session mode chip | The composer keeps the default dsh chrome: the omo role chip is gone; switching back restores it | aria, console | passing | dsh-v0.1.6-alpha.1 |
| `composer-role-picker` | web-client | opencode-omo session composer, left tool row (after the access/plan chips) | Chip accessible name is 角色：<displayName>; the menu lists exactly the five primary roles; the switch persists per session across a reload | aria, console, visual | passing | dsh-v0.1.6-alpha.1 |
| `role-model-application` | web-client | Role Settings primary model + composer role chip | The composer model control reports DeepSeek-V4-Pro: the role's pinned primary is applied to the session through the model RPC | aria, console | passing | dsh-v0.1.6-alpha.1 |
| `settings-section` | web-client | Settings dialog nav -> opencode-omo | Heading "opencode-omo", the alignment intro paragraph, and a tablist "opencode-omo 设置页" whose single 角色设置 tab is selected (the nested settings.tab slot) | aria, console, visual | passing | dsh-v0.1.6-alpha.1 |
| `role-catalog-rows` | web-client | Role Settings page | All 14 catalog roles render with display name, description, omo default chain, a centered 主模型 label, and one fallback "+" control each | aria, console | passing | dsh-v0.1.6-alpha.1 |
| `settings-primary-model` | web-client | Role Settings -> role row -> 主模型 dropdown | Menu offers 跟随当前 plus the four catalog models; the pinned model is durable across a reload and can be reset to 跟随当前 | aria, console | passing | dsh-v0.1.6-alpha.1 |
| `settings-fallback-add` | web-client | Role Settings -> role row -> circle "+" button | The panel opens below the role box and stays open for repeat additions; each pick becomes a removable chip; a duplicate is refused with 该模型已在 fallback 列表中 | aria, geometry:overflow, console | passing | dsh-v0.1.6-alpha.1 |
| `settings-fallback-cancel` | web-client | Role Settings -> open fallback panel | No fallback is added in any of the three cases | aria, console | passing | dsh-v0.1.6-alpha.1 |
| `settings-fallback-remove` | web-client | Role Settings -> fallback chip -> remove control | Chips disappear on click and stay gone after a reload | aria, console | passing | dsh-v0.1.6-alpha.1 |
| `role-settings-persistence` | web-client | Any accepted change on the Role Settings page | The opencode-omo-roles namespace carries the role-keyed fallback entry, and removal writes an empty list back | aria, console | passing | dsh-v0.1.6-alpha.1 |
| `role-settings-geometry` | web-client | Role Settings layout | Chip list and add panel stack below the role box at the row's full width without overlap; no horizontal overflow in the settings surface; the "+" control is a 28px circle; the last catalog row's panel is not sheared off by its scroll container | geometry:overflow, visual, console | passing | dsh-v0.1.6-alpha.1 |
| `omo-commands-catalog` | web-client | opencode-omo session composer, "/" trigger | handoff, hyperplan, refactor, remove-ai-slops, start-work, stop-continuation, and team-mode are all offered | aria, console | passing | dsh-v0.1.6-alpha.1 |
| `omo-skills-catalog` | web-client | opencode-omo session composer, "/" trigger | The omo skills published as user-dsh (ast-grep, coding-agent-sessions, programming, ulw-plan, ulw-research, visual-qa, ...) are discoverable | aria, console | passing | dsh-v0.1.6-alpha.1 |
| `omo-command-isolation` | web-client | Standard-mode session composer, "/" trigger | The omo commands and skills are absent (the catalogs are preset rows, not global) | aria, console | passing | dsh-v0.1.6-alpha.1 |
| `settings-rpc-fallback` | web-client | Non-loopback (memory-mode) browser: role settings fall back to the authenticated /opencode-omo channel | The channel answers a JSON server-response with ok:true and the role catalog. On dsh 0.1.6-alpha.1 the channel is never mounted: connection.rpc.handle() needs the caller to inject webServer, so registration throws and the request falls through to the SPA fallback (405). Pinned with test.fail() in the spec and asserted to fail by the run. | console | failing | - |
| `cli-bundle-composition` | cli | dsh --profile web --dump-config against the installed home | Exit 0; the tree contains the opencode-omo bundle row naming @royenheart/dsh-plugin-opencode-omo and the agent-presets roster row | console | passing | dsh-v0.1.6-alpha.1 |
| `cli-install-layout` | cli | install.py's documented install steps (symlink package, edit profile manifest, publish preset through $DSH_HOME/.agent-presets) | The profile lists the link: dependency and the bundle entry; $DSH_HOME/.agent-presets/opencode-omo is a real directory whose entries are symlinks into the checkout | console | passing | dsh-v0.1.6-alpha.1 |
| `opencode-toolchain` | headless | Model turn inside an opencode-omo session | The model-visible tool surface carries opencode's descriptions/parameters and the read/edit/write/web_search shims | aria | unknown | - |
| `omo-task-surface` | headless | Model turn calling task(...) inside an opencode-omo session | task(category/subagent_type/load_skills/run_in_background/task_id) maps onto named subagents/generic delegation and job notices use omo names | aria | unknown | - |
| `omo-multi-role-subagents` | headless | Delegation from an opencode-omo turn | oracle/librarian/explore/metis/momus/multimodal-looker plus generic subagent/subagent_fork/workflow/ralph are spawnable with the shipped personas | aria | unknown | - |
| `omo-context-injection` | headless | Session start in a workspace carrying AGENTS.md/CLAUDE.md/.omo rules | The walk-up instructions plus omo rules-injector text reach the assembled system prompt | aria | unknown | - |
| `omo-hooks` | headless | write/edit tool execution inside an opencode-omo turn | comment-checker rejects AI-slop comments and hashline tags reads / guards stale refs | aria | unknown | - |
| `per-mode-execution-backend` | headless | Tool execution inside an opencode-omo session | The session runs on the local filesystem backend plus a persistent PTY shell, isolated from other presets' sandboxed fs/shell | aria | unknown | - |
| `native-seam-loop-shim` | headless | Every model step of an opencode-omo session | Complete opencode+omo system prompt with live env block, gpt tool gating, ultrawork detection, maxSteps ceiling text (assistant prefill on a patched host, system section on stock), role model routing, and fallback advance on request errors | aria | unknown | - |
| `web-fetch-tool` | headless | Model turn calling web_fetch inside an opencode-omo session | The preset's fetch:true row exposes the native web_fetch tool without duplicating the base provider | aria | unknown | - |
| `lsp-tool-surface` | headless | Model turn calling lsp_* inside an opencode-omo session | The named lsp_* tools answer over the native lsp seam and degrade gracefully when no server binary is present | aria | unknown | - |
| `per-role-reasoning-effort` | headless | Role Settings reasoning-effort selector, then a model step | The selected effort is attached to the role's model selection and reaches the outgoing request | aria | unknown | - |
